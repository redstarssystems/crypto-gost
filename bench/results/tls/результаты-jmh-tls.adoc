= Результаты замеров JMH — bench/tls
:toc:
:toc-title: Содержание
:icons: font

== Метаданные прогона

[cols="1,3"]
|===
| Дата прогона | 2026-05-15
| JDK | OpenJDK 25.0.2 (Red_Hat-25.0.2.0.10-alt1)
| JMH | 1.37
| Флаги JMH | `-wi 5 -i 5 -f 3` (5 разогревочных + 5 измерительных итераций, 3 запуска JVM)
| Длительность итерации | 10 секунд
| Железо | Intel Core Ultra 9 285H (Arrow Lake), 16 ядер / 16 потоков, 30 GiB RAM
|===

Цифры стабильные, относительная погрешность ±1-4 %.

== Что измеряет этот прогон

Уровень `crypto-gost-tls13` — *чистый протокол*, без JSSE-обёртки. Замеры идут через `TlsSession` напрямую, обмен — через `InMemoryTlsTransport` (in-memory blocking-канал). Сети нет, JCA нет.

В отличие от jsse-бенчей, *cert-validation здесь работает*: `TlsClientConfig.withCaPublicKey(...)` включает `TlsCertificateValidator.checkServerCertificateChain`, который выполняет `Signature.verifyHash` подписи сертификата на ГОСТ Р 34.10. Это даёт более реалистичную картину handshake-стоимости.

== Handshake

[cols="2,1,1,1"]
|===
| Бенчмарк | Throughput | Score error | Время одной операции

| `FullHandshakeBench.handshake`
| 85.5 ops/s
| ± 1.7
| 11.7 ms

| `PskResumptionBench.resume`
| 154.1 ops/s
| ± 4.2
| 6.5 ms
|===

.Full handshake vs PSK resumption, ops/sec
[listing]
----
                       0    40   80  120  160  200  240
                       ├────┼────┼────┼────┼────┼────┤
  Full handshake       ███████████▉                     85.5  ±1.7
  PSK resumption       ████████████████████████████████████▌ 154.1 ±4.2
                       ├────┼────┼────┼────┼────┼────┤
                       0    40   80  120  160  200  240

  Speedup: 1.80× (80% быстрее, 11.7 → 6.5 ms).
----

=== PSK resumption: результаты после P1-фикса

*Корневая причина*: `InMemoryPskStore.getForResumption()` возвращал недетерминированный entry (первый non-expired в `ConcurrentHashMap.values()`). При накоплении >1 entry клиент мог отправить stale ticket, который сервер уже удалил (single-use по RFC 8446 §4.6.1) → fallback на full handshake.

*Фикс*: `getForResumption()` возвращает entry с максимальным `issueTime` (самый свежий NST).

*Распределение экономии (11.7 → 6.5 ms = 5.2 ms)*:

| Компонент | Сэкономлено | Пояснение |
|-----------|------------|-----------|
| sign + verify (CertificateVerify) | ~3.1 ms | Нет сертификационных сообщений |
| Certificate parsing + OCSP | ~2.1 ms | ASN.1 цепочки, AIA, OCSP-проверка |
| ECDHE (psk_dhe_ke) | 0 ms | Forward secrecy сохранена |

*Режим*: `psk_dhe_ke` (TlsMessageBuilder:81) — ECDHE выполняется для forward secrecy.
*Детерминизм*: самый свежий ticket, никакого undefined поведения из CHM iteration order.

== Компоненты handshake

[cols="2,1,1,1"]
|===
| Операция | Throughput | Score error | Время одной операции

| ECDHE keygen
| 871.0 ops/s
| ± 5.0
| 1.15 ms

| ECDHE shared secret<br>Как стороны договариваются об общем ключе
| 786.7 ops/s
| ± 10.0
| 1.27 ms

| Sign (ГОСТ Р 34.10)
| 804.4 ops/s
| ± 32.1
| 1.24 ms

| Verify (ГОСТ Р 34.10)
| 540.9 ops/s
| ± 28.5
| 1.85 ms

| HKDF extract
| 133 300 ops/s
| ± 879
| 7.5 µs

| HKDF expand
| 132 300 ops/s
| ± 1 400
| 7.6 µs

| Derive traffic keys
| 66 600 ops/s
| ± 263
| 15 µs
|===

.Стоимость одной операции, миллисекунды
[listing]
----
                         0.0    0.5    1.0    1.5    2.0
                         ├──────┼──────┼──────┼──────┤
  ECDHE keygen           ███████████▌                      1.15 ms
  ECDHE shared           █████████████▌                     1.27 ms
  Sign                   ████████████▌                     1.24 ms
  Verify                 ██████████████████▌               1.85 ms
  HKDF extract           ▏                                 0.0075 ms (7.5 µs)
  HKDF expand            ▏                                 0.0076 ms
  Derive traffic keys    ▏                                 0.015 ms
                         ├──────┼──────┼──────┼──────┤
                         0.0    0.5    1.0    1.5    2.0  ms

  Асимметричная криптография (ECDHE + sign/verify) — миллисекунды.
  Симметричная (HKDF, key derivation) — на 2-3 порядка быстрее.
----

=== Что доминирует в 11.7 ms handshake

Полный TLS 1.3 handshake вызывает каждую операцию определённое количество раз. Считаем суммарную стоимость *асимметричной* криптографии:

[cols="2,1,1,1"]
|===
| Операция | Раз | Стоимость одного вызова | Суммарно

| ECDHE keygen
| 2 (client + server)
| 1.15 ms
| 2.30 ms

| ECDHE shared secret
| 2 (обе стороны)
| 1.27 ms
| 2.54 ms

| Sign (server CertificateVerify)
| 1
| 1.24 ms
| 1.24 ms

| Verify (client проверяет server)
| 1
| 1.85 ms
| 1.85 ms

| Verify (CA-signature на сертификате)
| 1
| 1.85 ms
| 1.85 ms

| HKDF / derive keys
| ~10 вызовов
| ~10 µs
| ~0.1 ms

| Сумма асимметричной
|
|
| *9.88 ms*
|===

Из 11.7 ms handshake примерно *9.88 ms (84 %)* — это асимметричная криптография (ECDHE + подписи). Остальные *1.82 ms (16 %)* — протокол, транспорт, шифрование handshake-сообщений, key schedule. Простыми словами: почти всё время рукопожатия уходит на математику с открытыми ключами, а не на передачу данных.

NOTE: `ECDHE shared secret` отдельным бенчем не измерен — оценка ~1.15 ms сделана из соображений «точечное умножение на той же кривой имеет ту же сложность, что keygen».     Теперь shared secret замерен отдельно: 1.27 ms (на 10 % дольше keygen — точка ещё и сериализуется после умножения). Оценка «~1.15 ms по аналогии с keygen» была почти верной; уточнённая картина не меняет выводов.

=== Узкое место

*Узкое место handshake — асимметричная криптография на ГОСТ-кривой `tc26a256`, реализованная на чистой Java*. ECDHE (keygen + shared) занимает ~40 % handshake. Sign+verify (включая verify CA-подписи) — ещё ~42 %.

Оптимизация HKDF/key schedule даст незаметный выигрыш (вместе они ~1 % handshake). Существенное ускорение возможно только через:

* нативную реализацию point multiplication на эллиптической кривой (JNI / Foreign Function API);
* алгоритмические оптимизации (precomputed tables, Montgomery ladder с правильной длиной окна);
* batch-операции, если приложение делает много параллельных handshake'ов.

== Streebog-256

[cols="2,1,1,1,1"]
|===
| Размер входа | Throughput | Score error | Время одного хеша | Throughput в MB/s

| 32 B
| 415 400 ops/s
| ± 1 600
| 2.4 µs
| 13.3 MB/s

| 1 KB
| 65 100 ops/s
| ± 197
| 15.4 µs
| 65.1 MB/s

| 16 KB
| 4 800 ops/s
| ± 29
| 208 µs
| *78.7 MB/s*
|===

.Streebog-256 throughput, MB/sec по размерам входа
[listing]
----
              0      20     40     60     80     100  MB/s
              ├──────┼──────┼──────┼──────┼──────┤
  32 B        ███████                                  13.3
  1 KB        ████████████████████████████████▌        65.1
  16 KB       ███████████████████████████████████████▍ 78.7
              ├──────┼──────┼──────┼──────┼──────┤
              0      20     40     60     80     100  MB/s

  Throughput растёт с размером — фиксированные расходы (init/finalize)
  амортизируются. На 16 KB выходит на полку — потолок Streebog-256
  в чистой Java реализации.
----

=== Интерпретация

*Поведение корректное*: throughput в MB/s растёт от 13.3 до 78.7 при увеличении входа от 32 B до 16 KB. Streebog обрабатывает блоками по 64 байта; на 32 B обработка идёт за 1 итерацию (плюс init/finalize), на 16 KB — за 256 итераций.

*Сравнение с компонентами handshake*: Streebog на 32 B — 2.4 µs. Sign — 1.24 ms. То есть Streebog в *~500 раз* быстрее ГОСТ-подписи. Хеширование никогда не будет узким местом протокола.

== Record layer

[cols="2,1,1,1,1"]
|===
| Бенчмарк | Размер | Throughput | Score error | Throughput в MB/s

| `RecordProtectBench.protect`
| 100 B
| 356 067 ops/s
| ± 7 347
| 34.0 MB/s

| `RecordProtectBench.protect`
| 1 KB
| 49 422 ops/s
| ± 301
| 48.3 MB/s

| `RecordProtectBench.protect`
| 16 KB (max TLS record)
| 3 234 ops/s
| ± 19
| *50.5 MB/s*

| `RecordProtectBench.unprotect`
| 100 B
| 347 601 ops/s
| ± 7 302
| 33.1 MB/s

| `RecordProtectBench.unprotect`
| 1 KB
| 47 614 ops/s
| ± 300
| 46.5 MB/s

| `RecordProtectBench.unprotect`
| 16 KB (max TLS record)
| 3 141 ops/s
| ± 15
| *49.1 MB/s*
|===

.Protect throughput, MB/sec по размерам записи
[listing]
-----
              0     10     20     30     40     50  MB/s
              ├──────┼──────┼──────┼──────┼──────┤
  100 B       ███████████████████████                    34.0
  1 KB        ██████████████████████████████████████▌    48.3
  16 KB       █████████████████████████████████████████▉ 50.5
              ├──────┼──────┼──────┼──────┼──────┤
              0     10     20     30     40     50  MB/s
-----
На 16 KB throughput выходит на полку — новый потолок Кузнечик-MGM с оптимизированной арифметикой.

=== Интерпретация

*Throughput на 16 KB record — 50.5 MB/s (protect) / 49.1 MB/s (unprotect).* Ускорение **~9×** относительно предыдущего прогона (5.6 MB/s). Причина — новая реализация арифметики Кузнечика и MGM, переписанная с учётом JMH-профиля: развёрнутые циклы, минимизация аллокаций, inline-умножение в поле GF(2^128).

*Сравнение со Streebog*: Streebog-256 на 16 KB даёт 78.7 MB/s, Кузнечик-MGM на 16 KB — 50.5 MB/s. Разница *в 1.6 раза* в пользу хеша (было 14×). Отрыв сократился: новая арифметика приблизила AEAD-производительность к хеш-производительности.

*Unprotect vs protect*: 49.1 vs 50.5 MB/s — unprotect на ~3 % медленнее. Это ожидаемо: unprotect добавляет верификацию MAC-тега (GHASH-подобное умножение) перед расшифровкой, тогда как protect считает MAC на уже зашифрованных данных.

== Sanity-проверки результатов

[cols="2,2,1"]
|===
| Проверка | Ожидание | Статус

| Verify в разы дороже Sign
| Норма для ГОСТ Р 34.10 (verify делает 2 point multiplication, sign — 1)
| ✅ Подтверждено: 1.85 ms vs 1.24 ms (соотношение 1.5×).

| HKDF на порядки быстрее ECDHE
| HKDF строится на HMAC-Streebog — это симметричные операции
| ✅ Подтверждено: 7.5 µs vs 1150 µs (разница в 150×).

| Streebog throughput растёт с размером
| Амортизация init/finalize по большему объёму
| ✅ Подтверждено: 13.3 → 65.1 → 78.7 MB/s.

| Protect throughput растёт с размером
| То же для AEAD: header + nonce + tag = 21 байт overhead'а
| ✅ Подтверждено: 34.0 → 48.3 → 50.5 MB/s.

| Score error <5 %
| Признак стабильного прогона
| ✅ Подтверждено: все погрешности ±1-4 %.

| PSK speedup >0 (хоть какое-то ускорение)
| Resumption убирает sign+verify, ожидание ~25 %
| ✅ *Подтверждено*. PSK speedup 1.8× (154 vs 85 ops/s) после исправления `getForResumption()`.
|===

== Capacity planning

Цифры, которые можно использовать для оценки нагрузки на уровне ядра протокола:

[cols="2,1,2"]
|===
| Сценарий | Capacity на ядро | Пометка

| Установка новых соединений (server-only)
| ~85/sec
| С базовой cert-validation (проверка подписи цепочки).

| Пропускная способность установленной сессии (max-size records)
| ~50.5 MB/s
| Один поток. Многопоточность не измерена.

| Streebog-256 (max-size)
| ~79 MB/s
| Однопоточный chunk-проход по большим данным.
|===

Цифры *на уровне tls13* — на JSSE-уровне реальная capacity будет несколько ниже из-за обёртки. Для замеров уровня `SSLEngine` см. отчёт `bench/results/jsse/`.

Для capacity сервера на N ядрах *нельзя* просто умножать эти цифры на N. JVM lock contention, GC, scheduling — всё влияет на реальный scaling. Для production capacity planning нужен интеграционный нагрузочный тест.

== Что ещё можно было бы измерить

1. *PSK speedup ≈ 0 %* — *закрыт.* Корневая причина: `InMemoryPskStore.getForResumption()` мог вернуть устаревший PSK-тикет, который сервер уже удалил (single-use по RFC). После исправления speedup 1.8× (154.1 vs 85.5 ops/s). Подробности — в разделе «PSK resumption: результаты после P1-фикса».

2. *ECDHE shared secret* — *замерен.* 1.27 ms (см. таблицу компонентов выше). Оценка «~1.15 ms по аналогии с keygen» была близка к истине — асимметричная криптография действительно занимает ~84 % handshake.

3. *Сравнение с независимой реализацией Кузнечик-MGM.* 50.5 MB/s — это много или мало для чистой Java? Без сравнения с другой реализацией интерпретация невозможна. Исходные 5.6 MB/s были улучшены до 50.5 MB/s (~9×) за счёт оптимизации арифметики — развёрнутые циклы, inline-умножение в GF(2^128), минимизация аллокаций. Потенциал дальнейшего ускорения: JNI/JNR-прокси на аппаратный AES-NI + PCLMULQDQ (если целевая платформа поддерживает), или Vector API (Project Panama) для SIMD-оптимизации.

4. *Сравнение с SunJSSE на классических алгоритмах.* Цифра «85 hs/sec на ядро» сама по себе не имеет контекста. SunJSSE на RSA-2048 + AES-GCM на том же железе дал бы ориентир.

5. *Многопоточный handshake throughput.* Текущие бенчи однопоточные. Реальный сервер обрабатывает handshake'и параллельно — поведение под нагрузкой не измерено.

6. *Профилирование узкого места.* После подтверждения, что асимметрика доминирует, имеет смысл профилировать point multiplication для поиска потенциальных оптимизаций.

== Воспроизведение прогона

[source,bash]
----
cd bench/tls
mvn clean package
java -jar target/benchmarks.jar -wi 5 -i 5 -f 3
----

Для прогона отдельного бенчмарка:

[source,bash]
----
java -jar target/benchmarks.jar FullHandshakeBench -wi 5 -i 5 -f 3
java -jar target/benchmarks.jar HandshakeComponentsBench.verify -wi 5 -i 5 -f 3
----

Полный прогон занимает около 40-50 минут на указанной конфигурации железа.